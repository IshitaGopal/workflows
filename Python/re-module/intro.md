- Raw string - Just a string prefixed with an r. Tells Python not to handle back slashes in any special way. 


```python
text_to_search = 

print(r'\tTab') # prints \tTab. Does not remove \t

# Our patters
pattern = re.pattern('abc')

pattern.findinter()
```

- Meta characters need to be escaped 
. 

https://www.youtube.com/watch?v=K8L6KVGG-7o